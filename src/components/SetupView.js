import React from 'react';

export default function SetupView({ game }) {
  console.log(game);
  return (
    <>
      <p>
        Setupo view! will have a game started, but ships not placed, drag and
        drop here
      </p>
    </>
  );
}
